# 素材当前结果版本记录优化

## 本次完成

- 在图片制作的“当前结果”中直接增加版本记录，不再要求切换到生成作品后再进入详情查看。
- 同一创作的 V1、V2、V3 等版本按生成顺序横向排列，并显示对应执行方式（API 或 Codex）。
- 当前版本使用明确的选中状态。
- 点击任一旧版本会立即切换当前预览，并将它设为下一次继续调整或再生成的起点。
- 版本选择不会覆盖、复制或删除已有文件。

## 验证

- 已确认“概念版海报”现有 V1、V2、V3 都保存在独立文件中并归入同一版本组。
- ESLint 通过。
- Next.js 生产构建通过。
