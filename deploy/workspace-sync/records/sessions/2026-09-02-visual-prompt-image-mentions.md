# 画面目标支持图片引用

日期：2026-09-02

## 本次完成

- 将“画面目标”改为支持 `@图片编号` 的核心 Prompt 输入区。
- 生成新图、修改图片、内容配图和连续创作共用相同能力。
- 已选择的参考图片优先显示在 `@` 候选列表，其余图片仍可搜索选择。
- 从候选中选择图片会同时写入 Prompt 并加入本次参考图片集合。
- 生成请求会解析画面目标、补充要求和本轮调整中的图片编号。

## 验证

- 生产构建和 TypeScript 校验通过。
- 网页实测在画面目标输入 `@` 会显示图片候选；选择 `@IMG-012` 后，Prompt 正确写入编号，参考图片数量由 2 增加为 3。
- 生成新图、内容配图和连续创作均能看到同一个 `@图片` 画面目标输入区；修改图片使用相同公共表单。

## 未执行

- 未调用图片模型，未产生 API 费用或新增图片。
